# Spring MVC on Tomcat Container
## Add spring.profiles.active as environment with any value like stage/prod/test
## Package using mvn clean package command
## Copy the war to Tomcat's webapp directory
